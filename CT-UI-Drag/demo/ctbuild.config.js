const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctsj/ui-drag': path.resolve(__dirname, 'src/ctsj-ui-drag'),
      //   },
      // },
    };
  },
};
