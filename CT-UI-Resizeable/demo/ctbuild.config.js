const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctsj/ui-resizeable': path.resolve(__dirname, 'src/ctsj-ui-resizeable'),
      //   },
      // },
    };
  },
};
