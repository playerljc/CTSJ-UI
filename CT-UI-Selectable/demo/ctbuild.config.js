const path = require('path');

module.exports = {
  getConfig(config) {
    return {
      // resolve: {
      //   alias: {
      //     '@ctsj/ui-selectable': path.resolve(__dirname, 'src/ctsj-ui-selectable'),
      //   },
      // },
    };
  },
};
